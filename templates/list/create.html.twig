{% extends "base.html.twig" %}

{% block body %}
	{{form_start(form)}}
	{{form_row(form.name)}}
	{{form_rest(form)}}
	<input type="submit" value="Créer" class="btn btn-success">
	{{form_end(form)}}

{% endblock %}
